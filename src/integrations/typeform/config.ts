import { IIntegrationConfig } from '@useparagon/core/integration';

/**
 * configuration for a typeform
 */
const config: IIntegrationConfig = {
  description: 'Connect your Typeform account',
  overviewText: `Connect to your Typeform account to manage your forms and form responses in Typeform. Increase your team’s productivity by keeping your Typeform account up to date - without manual data entry.
   
Our Typeform integration enables you to:

  
• Sync Typeform form responses with this application
• Receive notifications when your forms receive responses`,
  showWatermark: false,
  workflowDisplayOrder: [],
};

export default config;
